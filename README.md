# Discord custom status animator
## Usage:
1. Edit the config.py to your liking.
2. Install the requirements ```python3 -m pip install -r requirements.txt```
3. Run the app with ```python3 animate.py```
